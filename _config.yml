# GitHub Pages Configuration for Vorchdorf App
# Note: .nojekyll is present, so Jekyll processing is disabled on GitHub Pages.
# This file is kept for reference or local development.

exclude:
  - README.md
  - .gitignore
  - node_modules
  - "*.backup"
  - scrapers/
  - backups/
  - "sicherung_*"
